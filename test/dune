; These are copies of the pin's suites linked only to mechanism's
; libraries.  The gate passes the pinned fixture directory explicitly.
(executables
 (names main wasm sl_surface)
 (modules Main Wasm Sl_surface Sys_io Kanon_kernel Kanon_surface Kanon_wasm)
 (libraries mechanism_kernel mechanism_surface mechanism_wasm))

(executable
 (name levels)
 (modules Levels)
 (libraries mechanism_kernel mechanism_surface))

(executable
 (name import)
 (modules Import)
 (libraries mechanism_import))

(executable
 (name import_types)
 (modules Import_types)
 (libraries mechanism_import mechanism_kernel))

(copy_files ../vendor/kanon/test/main.ml)
(copy_files ../vendor/kanon/test/wasm.ml)
(copy_files ../vendor/kanon/test/sl_surface.ml)
(copy_files ../vendor/kanon/test/sys_io.ml)
